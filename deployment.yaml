apiVersion: apps/v1
kind: Deployment
metadata:
  name: flask-k8s-cicd-demo
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
  selector:
    matchLabels:
      app: flask-k8s-cicd-demo
  template:
    metadata:
      labels:
        app: flask-k8s-cicd-demo
    spec:
      containers:
      - name: flask-k8s-cicd-demo
        image: k2009dutta/flask-k8s-cicd-demo:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 5000
---
apiVersion: v1
kind: Service
metadata:
  name: flask-k8s-cicd-demo
spec:
  type: NodePort
  selector:
    app: flask-k8s-cicd-demo
  ports:
  - port: 5000
    targetPort: 5000